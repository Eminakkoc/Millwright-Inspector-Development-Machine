Millwright-Inspector is a methodology for building software with AI coding agents. The **Millwright** (the AI agent) produces each step's work, and the **Inspector** (the human) reviews it and approves it to move forward. Every step leaves a Context Artifact — a markdown file that's both a document to review and the input the next step needs. It's tool-agnostic and adds structure to the way teams already build.

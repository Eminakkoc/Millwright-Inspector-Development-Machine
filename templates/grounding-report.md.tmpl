---
id: {{UUID}}
feature: {{FEATURE}}
seam-classification: {{SEAM_CLASSIFICATION}}
---

# Codebase-grounding report — {{FEATURE}}

Stage-2 audit artifact. Captured by `/mi-apply-impact` at stage-2 entry by a fresh sub-agent that walked the existing seam for each PENDING todo item this cycle. Used to compose the `requirements.md` body and to drive Step C's structural-diagram decision in `docs/blueprint-regeneration.md`.

> _Snapshot caveat: this report reflects the codebase at stage-2 entry. It is NOT regenerated mid-cycle; the actual implementation may diverge after stage 3. For mid-cycle drift, see `/mi-update-blueprint`._

<!-- Generated artifact — written by /mi-apply-impact's Step A grounding sub-agent.
     Archived to blueprints/history/v[N+1]/implementation/grounding-report.md at
     stage 8 by /mi-complete-workflow's archival loop. Treat inspector-review.md,
     change-summary.md, and the canonical blueprint files as the active source
     of truth; this is a one-shot snapshot to spare repeat codebase reads. -->

## Per-item findings

<!-- One subsection per todo item id in scope this cycle. Pattern:

### <ITEM-ID> — <one-line description>

- **Seam:** `<folder/path>` (existing files: `<file1>`, `<file2>`, ...).
- **Pre-existing components:** `<existing class/function/module the new code must integrate with>`. Empty list if none (greenfield case).
- **Cycle flavor:** greenfield | bugfix | improvement.
- **Shipped-code impact:** what already-shipped, working behavior this item changes or risks breaking — named call sites / consumers / persisted data / public contracts — and what must keep holding. `none — additive only (no existing caller reaches this path)` is a valid, expected answer for a genuinely greenfield item; write it explicitly rather than omitting the line.
- **Notes:** one or two lines on the integration shape, naming any constraints the requirements should respect.

Example:

### CART-001 — Add bulk add-to-cart

- **Seam:** `services/cart/` (existing files: `services/cart/CartService.ts`, `services/cart/CartRepository.ts`).
- **Pre-existing components:** `CartService.addItem(itemId, quantity)`, `CartRepository.upsertLine(...)`.
- **Cycle flavor:** improvement.
- **Shipped-code impact:** `CartService.addItem` is called from `routes/cart.ts` and `jobs/AbandonedCartJob.ts`; both pass a single item and depend on the current `{ ok, cartId }` return shape. Changing the signature in place would break both. The single-item path and its return shape must keep working unchanged.
- **Notes:** extends the existing single-item path; the bulk variant should reuse `CartRepository.upsertLine` per row to avoid duplicating idempotency logic.
-->

## Shipped-code regression risks

<!-- Cross-item view of what this cycle could break in code that is already shipped
     and working. One bullet per distinct risk (NOT one per item) — a single risk
     touched by three items is one bullet naming all three. Pattern:

- **<risk>** — `<symbol / route / table / contract>` is relied on by `<consumers>`;
  items <ITEM-ID>, <ITEM-ID> touch it. Must keep holding: <the behavior that cannot
  change>. Verify by: <the cheapest observable check — a route response, a log line,
  a DB row, an existing test>.

     Write `- (none — no in-scope item touches shipped behavior)` when the whole cycle
     is genuinely additive. An empty section is a defect, not an assertion of safety:
     main reads this section to write each Goals item's `**Shipped-code impact:**`
     bullet, and stage 5's manual-test plan turns these bullets into regression-seam
     scenarios. -->

## Overall seam summary

<!-- 2–4 bullets summarizing the cross-item picture:

- Dominant seam folder(s).
- Key existing components new code must integrate with.
- Classification rationale (why backend / frontend / mixed / infra).
- Anything the requirements author should be especially careful about (e.g., a load-bearing legacy module, a pending migration in the same area).

Keep this section concise — main reads it before writing requirements.md and uses it to anchor Goals to existing seams. Save longer notes for the per-item findings. -->

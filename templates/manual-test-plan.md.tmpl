---
id: {{PLAN_REVISION_ID}}
seed-family-id: {{SEED_FAMILY_ID}}
feature: {{FEATURE}}
generated-from-base-commit: {{BASE_COMMIT}}
generated-from-head: {{HEAD_AT_GENERATION}}
generated-against-run-root: {{RUN_ROOT}}
generated-in-activation: {{ACTIVATION_ID}}
{{REQUIREMENTS_FIELD}}
---

# Manual test plan — {{FEATURE}}

This plan was generated by `/mi-manual-test-plan` from the active feature's blueprint, change-summary, and a codebase scan rooted at the worktree path recorded in `generated-against-run-root`. The runner (`/mi-manual-test-run`) walks the scenarios in § 3 in order and asks the inspector for `pass`, `fail <observation>`, `skip <reason>`, `defer <reason>` (multi-feature cycles only, and never during the feature-test entry's own run), or `pause` per scenario.

## 1. Prerequisites

### 1.1. Services to run via docker-compose

{{SERVICES_BLOCK}}

### 1.2. Required env vars

{{ENV_VARS_BLOCK}}

### 1.3. Install + bootstrap

{{INSTALL_BLOCK}}

### 1.4. Seed data

{{SEED_BLOCK}}

## 2. What to run

<!--
Every command line in this section MUST be self-contained — copy-pasteable into the inspector's terminal without any preceding setup. The plan generator emits commands in one of two acceptable shapes (pick one per section, do not mix):

  1. Absolute-path inline (preferred):
       cd "/abs/path/to/.worktree" && pnpm dev

  2. Preamble export, then $RUN_ROOT references:
       export RUN_ROOT="/abs/path/to/.worktree"
       cd "$RUN_ROOT" && pnpm dev
       cd "$RUN_ROOT" && pnpm test:e2e

Bare `cd "$RUN_ROOT" && ...` without the export preamble in the same section is invalid (the variable would be undefined in the inspector's shell). When a specific command must instead run from the main checkout, the generator emits an absolute `cd "/abs/main/checkout"  # reason: <why>` for that one command.
-->

{{TERMINAL_COMMANDS}}

## 3. Test scenarios

Scenarios are grouped by letter (A, B, C, …) and numbered within (`A.1, A.2, B.1, …`). The runner walks them in order; the inspector answers `pass`, `fail <observation>`, `skip <reason>`, `defer <reason>` (multi-feature cycles only, and never during the feature-test entry's own run), or `pause` per scenario.

{{SCENARIOS}}

## 4. Coverage notes

<!--
Waived coverage-matrix cells — one line per Goal / changed area / matrix dimension
(edge cases, error paths, idempotency, regression seams, non-goal boundaries) that
intentionally has no scenario, with the reason. An empty section asserts full matrix
coverage. This section lives OUTSIDE § 3 so scenario-block parsers never mistake a
waiver line for a scenario.
-->

{{COVERAGE_NOTES}}
